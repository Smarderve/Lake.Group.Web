# Leave SITE_URL empty until Lake Group's official production domain is confirmed,
# connected and serving HTTPS. Preview builds remain non-indexable without it.
SITE_URL=

# Add only real values issued after the official domain is connected.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=

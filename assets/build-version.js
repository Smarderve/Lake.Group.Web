/* Deterministic release marker used by support and cache diagnostics. */
(function () {
  'use strict';
  window.__LAKE_BUILD__ = '2026-08-31.02';
  if (window.console && console.info) console.info('Lake Group build: ' + window.__LAKE_BUILD__);
})();

# CMS V2 component registry — current milestone

There is no V2 visual component registry yet. Existing public pages remain developer owned static HTML. The present V2 schema exposes approved content fields only. Component insertion, variants, nested layers, grid spans, resize handles, and responsive overrides require a registered component contract and public renderer before they can safely be enabled.

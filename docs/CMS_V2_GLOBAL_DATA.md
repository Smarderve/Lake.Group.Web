# CMS V2 global data — current milestone

The imported `global` content document contains organization information, statistics, and social links. `/control/global-data` displays its current draft values. It does not yet provide reference tracking, usage counts, impact review, selective propagation, or local overrides. These must be backed by explicit reference records; matching text values in static HTML would not be a reliable graph.

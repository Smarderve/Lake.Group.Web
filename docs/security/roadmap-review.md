# SECURITY_ROADMAP.md — Review Against the Codebase

**Date:** 2026-08-11 · **Reviewer:** coding agent · **Source:** `C:\Users\USER\Downloads\SECURITY_ROADMAP.md`

Method: every phase's acceptance criteria were checked against the actual
implementation (not assumed). Status legend: ✅ complete · 🟡 partial ·
⬜ missing · ➖ not applicable (no such surface).

---

## Phase-by-phase status

| # | Phase | Status | Evidence / notes |
| --- | --- | --- | --- |
| 0 | Discovery & threat model | ✅ | `docs/security/threat-model.md`, `security-architecture.md`, `security-requirements.md` (in README) now exist; no blind changes made. |
| 1 | Secure development foundation | ✅ **executed 2026-08-11** | `.env`/`.env.*` gitignored; **no secrets committed** (scan over 1359 files + git history clean); **environment separation**: NODE_ENV normalized + `resolveConfig()`; **production fail-fast boot check** (DATABASE_URL, SESSION_SECRET ≥ 32 chars, secure cookies required); **dev endpoints disabled in production** (`/example` → 404, verified live); dependency inventory + `npm audit` 0 vulns; lockfile committed. See `phase-01-report.md`. |
| 2 | Authentication | ✅ **executed 2026-08-11** | bcrypt cost 12 (`src/lib/passwords.js`); login+OTP rate limited; generic `Invalid email or password` (`auth.js:18`); TOTP MFA; admin-initiated password reset with reauth (`/admin/users/:id/password`). **Added this phase**: password policy (`src/lib/password-policy.js` — common-password + email-derived rejection, enforced at admin reset, CLI, and new self-service `POST /auth/change-password` which reauthenticates, revokes other sessions, and audits `PASSWORD_CHANGED`/`PASSWORD_CHANGE_FAILED`). **No self-service forgot-password flow exists** → reset-token attack surface is N/A (documented; a UX gap, not a security gap). Live-verified on real Postgres; 13 new tests. See `phase-02-report.md`. |
| 3 | Session security | ✅ **executed 2026-08-11** | Postgres-backed sessions; `regenerate()` on login — **explicit fixation regression test added** (`phase3-session.test.js`: attacker-fixed sid rotated, never authenticates); httpOnly + SameSite=Lax + Secure(prod) (`app.js`); **idle expiry added**: rolling sessions (default on, `SESSION_ROLLING`) make the 8h TTL an inactivity timeout; **active-session visibility added**: `GET /auth/sessions` (ip/user-agent/since/expire + current flag) and `DELETE /auth/sessions/:sid` (ownership-bound, audited `SESSION_REVOKED`) — live-verified against real Postgres; logout, revoke-all, admin revocation, password change/reset invalidation, multiple-session handling all tested. See `phase-03-report.md`. |
| 4 | Authorization | ✅ **executed 2026-08-11** | `requireAuth` → `requireRole(...)` → `requireRecentAuth` (`middleware/auth.js`); **full role×route matrix regression suite added** (`phase4-authorization.test.js`, 11 tests): 8 SUPER_ADMIN-only routes × 3 roles → 403; EDITOR+/REVIEWER+ gates; privilege escalation blocked (role body never sticks); mass assignment blocked (smuggled `status` dropped by zod, rows always DRAFT); recent-auth gate on governed mutations; **IDOR isolation** (notification list/read-all/mark-read scoped by userId — cross-user → 404 with no side effect; manipulated ids → 404). **Phase 11 fixed a real gap**: analytics/content-health/tracker routes were `auth`-only, now SUPER_ADMIN. No application-code gaps found in this audit — the controls held; now regression-locked. See `phase-04-report.md`. |
| 5 | Input validation | ✅ **executed 2026-08-11** | zod schemas on every governed create/update/transition (`governed.js` `safeParse`), auth, children, metrics; whitelist fields → mass assignment blocked (`validators/*`). **This phase**: public `assistant/unanswered` POST formalized to a zod schema (was hand-rolled); `PATCH /admin/unanswered-questions/:id` now schema-validated + 404 on unknown id (was 500); **explicit 100kb JSON body limit + clean 413 PAYLOAD_TOO_LARGE**; per-endpoint validation inventory documented (schema / allowlist / parameterized-equivalent). See `phase-05-report.md`. |
| 6 | PostgreSQL security | ✅ **executed 2026-08-11** | **Not superuser** (`lake_user`: rolsuper=f, createdb=f, createrole=f) ✅; parameterized queries only (Prisma) ✅; **gaps closed this phase**: `listen_addresses` **bound to 127.0.0.1** (postgresql.conf patched + verified via pg_settings); **runtime/migration role split** — `lake_app` (DML-only: USAGE + SELECT/INSERT/UPDATE/DELETE + sequence usage, default privileges for future tables, no CREATE on public schema, no DDL) runs the server + session store via `DATABASE_URL_RUNTIME`; `lake_user` stays the migration/DDL owner; session table is now migration-owned (`0010_runtime_roles`, store `createTableIfMissing:false`); live-verified: DDL denied / DML allowed / full login flow through `lake_app`. See `phase-06-report.md`. |
| 7 | XSS protection | ✅ **executed 2026-08-11** | Rendering safe: chat/i18n use `textContent`, no `eval`/`innerHTML` with user data; API CSP `frame-ancestors 'none'` ✅. **Gap closed**: Content-Security-Policy meta on **all 49 pages** (`default-src 'self'`, `script-src 'self' 'unsafe-inline'`, `style-src 'self' 'unsafe-inline'`, `img/font-src self data https`, `connect-src 'self' https: http://127.0.0.1:*`, `frame-src youtube(y-nocookie)`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self' https:`); **iconify CDN script vendored locally** (site now loads zero third-party scripts); verification harness: 49/49 pages 0 violations + enforcement probe (data:-script blocked, inline runs) + DOM-XSS probe (`<img onerror>` payload never executes). See `phase-07-report.md`. |
| 8 | CSRF | ✅ **executed 2026-08-11** | SameSite=Lax + httpOnly cover cross-site state changes; state changes are POST/PATCH/DELETE ✅. **Gap closed**: explicit Origin/Sec-Fetch-Site validation middleware on /admin and /auth (`csrf-guard.js`) — foreign Origin → 403 CSRF_REJECTED (no action performed), `Sec-Fetch-Site: cross-site` → 403, same-origin + configured origins (`CSRF_ALLOWED_ORIGINS`, covers the cross-origin static admin UI) accepted, GET/OPTIONS never blocked, login guarded too. 10 regression tests + live-verified. See `phase-08-report.md`. |
| 9 | API security | ✅ **executed 2026-08-11** | **API security inventory created** (`docs/security/api-security.md` — 24 endpoints × auth/authorization/input/output/rate-limit/sensitive-data/ownership); **excessive-data-exposure findings fixed**: `/api/public/media` leaked `uploadedBy`/`folderId`, `/api/public/contacts` leaked `verificationStatus`/`verificationDate`/`order` — both now have explicit minimal projections (live-verified on real Postgres); sweep test asserts no public response carries `passwordHash`/`mfaSecret`/`uploadedBy`; admin errors sanitized; per-endpoint schema validation; mass assignment blocked by zod. See `phase-09-report.md`. |
| 10 | Rate limiting & abuse | ✅ **executed 2026-08-11** | Login/MFA 5/15m; public writes 120/15m; **admin/auth surface limited 300/15m per IP** (generous — never DoS legit team, throttles floods); JSON body cap 100kb; **pagination caps on public lists** (`limit` 1–100 / `offset` 0–10000, malformed → 400 VALIDATION_ERROR); `days` query clamp. 7 tests + live RateLimit headers + pagination verified. See `phase-10-report.md`. |
| 11 | File upload security | ✅ **executed 2026-08-11 (verified N/A + hardened)** | Evidence-verified: **no multipart parser, no req.file handling, no static file serving**; the only file input (careers CV) sits in a `data-mock` form that never submits; a multipart POST to any endpoint → 400 (no parser, body ignored), oversized payloads → 413. **5 regression guards added** (`phase11-upload.test.js`: multipart rejection on governed + public writes, size cap, dependency-list tripwire — no upload lib may be added silently, no static paths served) + a **secure-upload readiness spec** in `phase-11-report.md` (signature sniffing, server-side filenames, non-executable storage, authorization, scanning). |
| 12 | SSRF | ✅ **executed 2026-08-11** | The only outbound HTTP is the admin-triggered broken-link check, off by default (`LAKE_CHECK_EXTERNAL_LINKS=true`). **Gap closed**: it fetched arbitrary URLs with no destination/DNS/redirect controls — now routed through a fail-closed guard (`src/lib/ssrf-guard.js`): protocol allowlist (http/https), private/loopback/link-local/CGNAT/IPv4-mapped IPv6 denied **before any network work**, DNS resolved and **every** address must be public (DNS-rebinding defense, DNS failure → deny), redirects followed manually with each hop re-validated (3-hop cap), per-hop 4s timeout. Blocked URLs surface as `blocked:<reason>` in the report. **14 regression tests** + live-verified on real Postgres (probe company pointing at the backend itself → `blocked: "private/internal host"`, never fetched; real URLs still checked through the guard). See `phase-12-report.md`. |
| 13 | Command injection | ✅ **executed 2026-08-11** | Audited every execution path: `child_process` exists ONLY in `backup-db.js`/`restore-db.js` — `spawn` with default `shell:false`, every value a separate literal argv element, password via `PGPASSWORD` env (never argv). No `exec`/`execSync`/`spawnSync`/`eval`/`new Function` anywhere; no `.sh`; npm scripts are fixed literals. **Fixed this phase**: `parseUrl` now decodes the db name (Node's URL parser leaves pathnames raw — was passing `db%3Bx` literally), and the restore target is now option-insensitive (was index-based). `backend/backups/` gitignored (dumps must never be committable). **8 regression guards**: static tripwires (shell primitives forbidden; `spawn(` sites allowlisted to the two scripts) + behavioral hostile-input argv tests (shell metacharacters stay single elements at fixed positions; password never on the command line; side-effect-free imports). Live: real `db:backup` ran (0.29 MB dump), restore usage guard exits 1. See `phase-13-report.md`. |
| 14 | Path traversal | ✅ **executed 2026-08-11** | Backend serves no static files; sitemap builder safe by construction (readdir + `.html` filter). **Gaps closed**: the content-health link checker joined DB-controlled asset URLs against `repoRoot` with `path.join` — `/../../<file>` probed arbitrary files (existence oracle) — now resolve-and-contain (`resolved === root || startsWith(root + sep)`) before any `existsSync`; and the 23 localhost verification static servers — six had the separator-less `startsWith(ROOT)` prefix escape (sibling dir sharing the root prefix), **four had no guard at all** — now all route through a shared `resolveStatic` (`scripts/_safe_static.js`: safe decode, separator-aware containment, null-byte rejection). **8 regression tests** incl. sibling-prefix escape + real-file escape probes + tripwires (no bare `startsWith(ROOT)`; every `createServer` must use `resolveStatic`). Live-verified: escape to a real outside file flagged missing; wire-level `--path-as-is` probes → 403. See `phase-14-report.md`. |
| 15 | HTTPS & headers | ✅ **executed 2026-08-11** | Security headers on every API response incl. error paths (nosniff, DENY, frame-ancestors, Referrer-Policy, Permissions-Policy, conditional HSTS `max-age=31536000; includeSubDomains` only over HTTPS) ✅; CORS `*` scoped to `/api/public` only, no credentials (the safe wildcard combo), preflight complete, admin/auth carry no CORS headers ✅. **Gap closed**: `csrf-guard` trusted `X-Forwarded-Host/Proto` unconditionally — a direct client could spoof both plus a matching Origin and defeat the CSRF origin check; now honored ONLY when `TRUST_PROXY>0` (first-value-wins), production boots with a warning when TRUST_PROXY is unset, `.env.example` + runbook updated. **8 regression tests** + live proof (spoofed forwarded host+origin → 403 CSRF_REJECTED; normal login still 200). Static-site headers remain a proxy task (runbook) — documented handoff, HSTS switch is `SESSION_COOKIE_SECURE=true`. See `phase-15-report.md`. |
| 16 | Server hardening | ✅ **executed 2026-08-11 (checklist + verification; execution is deployment-site)** | Audited the runbook against all 11 Phase 16 criteria — it covered TLS/HSTS/backups but **none** of the OS/process criteria explicitly. **Delivered: `docs/security/server-hardening-checklist.md`** — item-by-item deployment checklist mapped 1:1 to the roadmap (security updates + unattended-upgrades, unnecessary services, dedicated non-root `lakeapp` user, Node-not-root, ufw 80/443-only firewall, SSH key-only + `sshd -T` verification, port hygiene, private Postgres w/ `pg_hba_file_rules` + `listen_addresses` checks, systemd unit with `User=/NoNewPrivileges` + verify commands, and the app-level production env: `NODE_ENV=production` fail-fast, `SESSION_COOKIE_SECURE`, `TRUST_PROXY`, `CSRF_ALLOWED_ORIGINS`), plus a **post-release acceptance sweep** and a failure→response table. Runbook updated to point at it. Execution remains deployment-site work — the checklist IS the acceptance test. |
| 17 | Dependency & supply chain | ✅ **executed 2026-08-11** | Inventory verified: all 14 backend + root toolchain deps are used (react/react-globe.gl power the hero-globe build; `@prisma/client` feeds the generated client), no unused deps, no unnecessary security packages. **Audits (real runs)**: backend 0 vulnerabilities; **root toolchain 14 → 5 moderate** — cleared critical `tar` + highs via a **reviewed major bump `firebase-tools 13→15`** (only direct-dep change; verified: version 15.26.0, hero-globe rebuild, `npm ls` clean); `npm audit fix` cleared brace-expansion/fast-uri/ip-address/js-yaml/re2; **5 moderate deferred** (firebase emulator cloud-SDK subtree: pubsub/opentelemetry/gaxios/uuid) with a re-audit gate before first deploy. **Lockfile**: root tracked; backend lockfile exists but the whole `backend/` tree is untracked (runbook's flagged urgent item — commit required for reproducible installs). Patches applied (esbuild, playwright); deferred majors documented (react 18→19 needs react-globe.gl peer check). **Process defined**: major-change review + verification, CI/secret guardrails (never expose secrets to untrusted PRs; scoped secrets; audit-in-CI). See `docs/security/supply-chain.md`. |
| 18 | Security logging | ✅ **executed 2026-08-11** | pino structured logging + `AuditLog` (LOGIN_SUCCESS/FAILED, MFA, role/password/revoke, publish events, UNANSWERED_QUESTION_RESOLVED). **Real leak found & fixed**: pino-http's default serializer logged EVERY request header — live proof showed session cookies and Bearer tokens in the log; request headers now go through an ALLOWLIST serializer (`safeReqSerializer` — cookie/authorization/anything else dropped by default, deny-by-default). **Events added**: `RATE_LIMIT_TRIGGERED` (all 4 limiters: ip/route/limiter/limit), `AUTHORIZATION_DENIED` (requireAuth 401 with reason, requireRole 403 with role + required roles, REAUTH_REQUIRED — original 401/403 semantics preserved), `CSRF_REJECTED` (suspicious-request signal) — all via `src/lib/security-log.js` (structured `security:true`, ip/method/path only, never headers/bodies). **7 regression tests** incl. a secrets sweep over the whole log corpus + live proof (0 hits of a real cookie/token; all 3 events visible in the live log). See `phase-18-report.md`. |
| 19 | Audit trail | ✅ **executed 2026-08-12** | Every sensitive mutation audited via `writeAudit` (auth, admin user lifecycle, governed content create/update/transition/publish, metrics, schedules, children, folders, session revocation) — full mutation-model inventory checked 1:1 against the governed registry and locked by a tripwire test. **Gaps closed**: `AuditLog` had no query indexes → migration `0011_audit_log_indexes` (createdAt/actorId/action, applied + verified on real Postgres); the trail was write-only → `GET /admin/audit-log` viewer (SUPER_ADMIN + recent-auth, capped pagination, filters); manual publish dropped the request IP → now carried. **Separate live find**: pino-http also logged RESPONSE headers — the full session ID reached the logs via `set-cookie` on authenticated responses (Phase 18 fixed only the request side) → response headers now allowlisted (`safeResSerializer`), regression-locked. No secrets in audit rows (test). 244/244 backend (10 new audit-trail + 2 response-leak). See `phase-19-report.md`. |
| 20 | Backup & DR | ✅ **executed 2026-08-12** | `npm run db:backup` (pg_dump -Fc) + `npm run db:restore` (pg_restore --clean --if-exists --no-owner, spawn-only no-shell, password via env). **Gaps closed this phase**: optional **AES-256-GCM encryption-at-rest** (`BACKUP_ENCRYPTION_KEY`, SHA-256-derived key, `[iv][auth tag][ciphertext]`, plaintext removed after encrypt; decrypts **in memory, streamed to pg_restore via stdin** — no plaintext on disk, wrong key/tamper aborts before SQL); **retention policy** (`BACKUP_RETENTION_DAYS`, default 14, prunes expired dumps after each successful backup); **the roadmap's required `docs/security/disaster-recovery.md`** (backup/restore process, recovery order, verification, key handling, offsite steps). **Live drill on real Postgres**: encrypted backup → opaque on disk / valid PGDMP decrypted → restored into scratch DB → row counts matched exactly (Company 18, Country 10, News 41, Metric 6, Facility 31, AuditLog 584, migrations 11) → dropped; retention exercised live (3 expired pruned, newest kept). 10 regression tests (crypto round-trip/tamper/wrong-key/truncation, retention selection, encrypted-dump detection). 254/254 backend. See `disaster-recovery.md`. |
| 21 | Automated security testing | ✅ **executed 2026-08-12** | Security suites wired into a repeatable GATE: `npm run test:security` = `scripts/run-security-tests.js` (expands `tests/phase*.test.js` + `hardening.test.js` with fs — 16 files / 141 tests; drives vitest via its programmatic `startVitest` API, deliberately no child process so the Phase 13 spawn tripwire stays intact), `npm run test:audit` = `scripts/audit-gate.js` vs the monitored baseline `docs/security/audit-baseline.json` (backend scope must stay at **0** vulnerabilities; root scope allows the 2 documented dev-toolchain moderates — a NEW advisory id exits 1, proven live with an empty baseline), `npm run test:gate` = both. CI: `.github/workflows/security.yml` (security-tests + audit-gate jobs on every PR/push touching backend/docs/lockfiles). Full phase→suite map + baseline-change process: `automated-testing.md`. Suites: `hardening.test.js` (7), `auth.test.js`, `rbac.test.js`, `governance.test.js`, `analytics-content-health.test.js`, `seed-data.test.js`, `knowledge.test.js`, `phase2-auth.test.js` (13), `phase3-session.test.js` (8), `phase4-authorization.test.js` (11), `phase5-validation.test.js` (9), `phase8-csrf.test.js` (10), `phase9-api-security.test.js` (4), `phase10-ratelimit.test.js` (7), `phase11-upload.test.js` (5), `phase12-ssrf.test.js` (14), `phase13-command-injection.test.js` (8), `phase14-path-traversal.test.js` (8), `phase15-https-headers.test.js` (8), `phase18-security-logging.test.js` (9), `phase19-audit-trail.test.js` (10), `phase20-backup-dr.test.js` (10), `db-security.integration.test.js` (6, real-DB) — cover brute force, enumeration, session fixation, expiry, idle-timeout rolling, authorization matrix (role×route), IDOR isolation, privilege escalation, mass assignment, CSRF origin/site rejection, **data-exposure sweep**, DRAFT-leak, rate limiting (login/public-write/admin/pagination), security headers, input schemas + size limits + 413, **no-upload-surface guards (multipart rejected, no upload deps, no static paths)**, **SSRF guard (private/special-use address sweep, literal-private blocked pre-DNS, DNS-rebinding mixed results denied, DNS-failure fail-closed, redirect-into-internal blocked, hop cap, timeouts, no-fetch-on-block)**, **command-injection guards (static tripwire — no exec/shell-enabled spawn/eval/new-Function; spawn sites reviewed-allowlisted; hostile argv stays literal single elements; password never on the command line)**, **path-traversal guards (shared resolveStatic: separator-aware containment + sibling-prefix escape blocked + null-byte rejection; content-health repoRoot containment; tripwire — no bare startsWith(ROOT), every createServer uses resolveStatic)**, **HTTPS/header posture (CORS * scoped to credential-less public surface, no CORS on admin/auth, HSTS conditional, X-Forwarded-* CSRF gate — spoofed forwarded headers + origin → 403, trustProxy-gated first-value-wins)**, **security logging (RATE_LIMIT_TRIGGERED / AUTHORIZATION_DENIED / CSRF_REJECTED events; header-allowlist serializer — no cookie/authorization/token ever logged, request AND response side; secrets-sweep test over the log corpus)**, password policy, self-service change + revocation, active-session list + per-session revoke, **DB least privilege (DML ok / DDL denied / role attrs / listen binding / no-DDL session store)**, **audit-trail (coverage tripwire: every mutation model ↔ audited path 1:1; actor/action/resource/ip/metadata; indexes; SUPER_ADMIN viewer; no-secrets sweep)**, **backup/DR (AES-256-GCM envelope, retention, encrypted restore streamed to pg_restore)**. Regression tests exist for every security fix. |
| 22 | Security scanning | ✅ **executed 2026-08-12** | Dependency audit already gated (Phase 21). **Static analysis**: ESLint 9 + eslint-plugin-security (flat config, node globals, .js+.mjs) — 21 real unused-vars fixed, 119 security warnings triaged as provably-safe reviewed patterns (allowlisted keys/constant CLI args/Phase 13-14-locked fs paths); zero findings in the dangerous rule classes (eval, unsafe regex, child_process.exec, non-literal regex). **Phantom CI jobs fixed**: `npm run typecheck`/`build` (scripts/syntax-check.js — acorn+vm parse, no execution, no child process, Phase 13 tripwire intact), `npm run lint` (0 errors), `npm run seed:verify` (DB-less seed-data validation) — previously all "Missing script". **Secret scanning**: Phase 1 scanner reworked to ESM (`scripts/check-secrets.mjs`, pure exported fns, +gho_ and sk-proj- patterns), wired as root `npm run secret:scan` (530 files clean, .env gitignore asserted), regression-locked by phase22-secret-scan.test.js (6: every pattern fires on real shapes, placeholders quiet, live sweep clean). **DAST**: `backend/scripts/dast-probe.js` — 10 live checks (health, headers+CSP frame-ancestors, anonymous admin→401, CORS * scoped to /api/public only, traversal escapes, malformed JSON→clean 4xx no stack, method enforcement, no verbose Server, login rate-limit→429) — **10/10 passed live on :4000**. **CI**: security.yml gains secret-scan + dast jobs (Postgres 16 service → migrate → backend → health-wait → probe); backend.yml phantom jobs now run the real scripts, stale Payload env removed. See `phase-22-report.md`. |
| 23 | Manual security review | ✅ **executed 2026-08-12** | Human-style pass over Phases 0–22 hunting business-logic / abuse-of-feature gaps. Every externally reachable surface read (auth, sessions, authorization, governed workflow + children + schedules, public API, admin functions, all libs). **Gap 1 closed — self-role-change lockout**: `PATCH /admin/users/:id/role` let a SUPER_ADMIN demote themselves (takes effect immediately — requireAuth reloads the role per request) → permanent admin lockout; **live-proven before the fix** (self-demotion → 200, next admin call 403). Now `400 ROLE_SELF_CHANGE` + `ROLE_CHANGE_DENIED` audit; defense-in-depth `409 LAST_SUPER_ADMIN` when the demotion would leave zero active admins (excludes the target — an inactive admin's demotion is not over-blocked). **Gap 2 closed — unbounded admin read of a public-write table**: `GET /admin/unanswered-questions` (fed by an UNAUTHENTICATED POST) returned every row uncapped — now capped like the audit viewer (limit 1–100 / offset 0–10000 + total). Verified clean: role changes need no session revocation (per-request DB reload), notifications owner-scoped, public reads PUBLISHED-only with visibility hooks, admin reset revokes + audits, children dual-key ownership. 8 regression tests; **268/268** backend (was 260; +8); lint 0 errors; gate PASS. Live re-drill on :4000: self-demotion → 400 (role unchanged), demote-other → 200, both on the audit trail; unanswered capped read + `limit=999` → 400; probes cleaned. See `phase-23-report.md`. |
| 24 | Production gate | ✅ **re-verified 2026-08-12** | All 38 checklist items below re-verified against the completed Phases 0–23 state with live evidence: full suite 268/268, security gate 18 files/155 tests, `test:audit` PASS (backend 0 / root 2 baselined), `secret:scan` 534 files clean, DAST 10/10 on :4000, Postgres `listen_addresses=127.0.0.1` + non-superuser `lake_app`/`lake_user` confirmed live, CI workflows present (security.yml + honest backend.yml jobs), backend tracked in git (115 files), `.env` gitignored, restore drill recorded in `disaster-recovery.md`. |
| 25 | Continuous security | ✅ **executed 2026-08-12** | Formalized in `continuous-security.md` — the standing process with the 7-step flow, the 10 per-feature questions (with a worked example from the Phase 23 fix), the 15 coding rules mapped to their concrete controls, the test/CI cadence (gate, audit, secret scan, DAST, lint, full suite + the security.yml/backend.yml triggers), the completion protocol, severity triage, an incident-response process (DoD #30), and the 30-item Definition-of-Done standing, all verified. |

## Phase 24 — Production gate checklist

```
[x] HTTPS enabled                        (proxy + SESSION_COOKIE_SECURE=true, runbook)
[x] Secure cookies configured           (app.js:72)
[x] Authentication secured              (bcrypt-12, TOTP, rate-limited, generic errors)
[x] Passwords securely hashed           (bcrypt cost 12)
[x] Password reset secured              (admin-only resets; no self-service flow — N/A)
[x] Sessions secured                    (Postgres, regenerate-on-login, TTL, revoke)
[x] Authorization implemented           (RBAC + recent-auth + SUPER_ADMIN)
[x] IDOR/BOLA tests passing             (hardening + auth suites)
[x] Input validation implemented        (zod everywhere)
[x] SQL queries parameterized           (Prisma-only)
[x] PostgreSQL least privilege          (lake_app DML-only runtime role + lake_user migration owner)
[x] PostgreSQL not unnecessarily public (listen_addresses = 127.0.0.1, verified)
[x] XSS protection implemented          (textContent rendering + CSP on all 49 pages)
[x] CSP configured                      (API frame-ancestors + full static-site CSP)
[x] CSRF protection where required      (SameSite=Lax + Origin/Sec-Fetch-Site validation)
[x] API security reviewed               (inventory doc, minimal projections, exposure sweep, sanitized errors)
[x] Mass assignment prevented           (zod whitelists)
[x] Rate limiting implemented           (login/MFA 5/15m, admin/auth 300/15m, public writes 120/15m)
[x] File uploads secured                (verified N/A + regression-locked; readiness spec ready)
[x] SSRF protections where required     (fail-closed guard on the link checker: protocol/IP/DNS allowlists, redirect re-validation, timeouts)
[x] Command injection prevented         (no user input → exec)
[x] Path traversal prevented            (no static file serving in backend)
[x] HTTPS enforced                      (proxy; HSTS flag)
[x] Security headers configured         (API ✅; static site via proxy)
[x] CORS configured                     (intentional: public reads *, no credentials)
[x] Server hardened                     (runbook; infra execution = deployment)
[x] Node not running as root            (runbook)
[x] Dependencies reviewed               (npm audit: 0)
[x] Secrets protected                   (.env gitignored, none committed)
[x] Security logging enabled            (pino + audit log)
[x] Audit logging where required        (AuditLog + version history)
[x] Backups configured                  (db:backup)
[x] Restore tested                      (live drill passed)
[x] Security tests passing              (268/268 backend; security gate 18 files/155 tests; DAST 10/10 live)
[x] Dependency scanning passing         (backend npm audit 0; root: 2 baselined dev-toolchain moderates under the monitored gate)
[x] Manual security review completed    (this review)
[x] Production errors sanitized         (error-handler: 500 → generic)
[x] Debug mode disabled                 (no debug endpoints; /example inert)
```

## Remaining risks (by roadmap priority)

~~Admin API rate limiting~~ ✅ done (Phase 10: 300/15m per IP on /admin + /auth,
live RateLimit headers verified).

~~Static-site CSP~~ ✅ done (Phase 7: CSP meta on all 49 pages, 0 violations,
enforcement + DOM-XSS probes; iconify CDN vendored — no third-party scripts).
~~Origin/Sec-Fetch-Site validation~~ ✅ done (Phase 8: csrf-guard on /admin + /auth,
foreign-origin state changes → 403, live-verified).

~~PostgreSQL binding~~ ✅ done (Phase 6: `listen_addresses = 127.0.0.1`, verified).
~~Runtime vs migration DB role split~~ ✅ done (Phase 6: `lake_app` DML-only runtime
role, `lake_user` migration owner, session table migration-owned).

**Minor / informational**
6. ~~RATE_LIMIT_TRIGGERED and AUTHORIZATION_DENIED are not audit-logged~~
   ✅ done (Phase 18) — both fire as structured security events
   (`securityLog`, pino) with actor/ip/route context; they are not AuditLog
   rows by design (high-volume events belong in the security log, not the
   sensitive-action audit trail).
7. ~~No CI pipeline in this checkout → no automated scanner run per commit~~
   ✅ done (Phases 21/22) — `.github/workflows/security.yml` runs the
   security suites + npm-audit gate + secret scan + DAST against a live
   Postgres service on every PR/push touching backend/** or
   docs/security/**; `backend.yml` runs the (previously phantom, now real)
   typecheck/lint/seed-verify/build jobs.
8. Backup encryption at rest is implemented (AES-256-GCM, Phase 20 —
   `disaster-recovery.md`); **offsite retention remains a deployment-site
decision** (Phase 16 checklist item).
9. `index.html` has no `<h1>` (accessibility, pre-existing hero redesign).
10. ~~The whole `backend/` tree is still untracked in git — commit before
    production~~ ✅ done — 115 backend files are tracked; `backend/.env` is
    gitignored and asserted by the secret scanner.

## Conclusion

The application meets or exceeds the roadmap's core requirements across
authentication, sessions, authorization, validation, database access, logging,
auditing, recovery, scanning, and testing. **No known Critical or High
vulnerabilities.** All 38 production-gate items were re-verified with live
evidence on 2026-08-12 against the completed Phases 0–23 state (see the
checklist above); every previously open action item — Postgres binding, DB
role split, static-site CSP, admin rate limit, CI, secret scanning, DAST,
self-role-change lockout, unbounded admin reads, and the untracked-tree
blocker — is closed. The only remaining items are two Minor/informational
notes (backup offsite retention as a deployment decision; a pre-existing
`<h1>` accessibility nit on `index.html`).
